suppressPackageStartupMessages(library(metafor))
d <- read.csv(commandArgs(trailingOnly = TRUE)[1])
r <- rma.uni(yi = d$yi, sei = d$sei, method = "REML")
pet <- regtest(r, model = "lm", predictor = "sei", ret.fit = TRUE)
cat("summary coefficients:\n")
print(summary(pet$fit)$coefficients)
cat("vcov:\n")
print(vcov(pet$fit))
cat("SE from summary:\n")
print(summary(pet$fit)$coefficients[, "Std. Error"])
