# Pairwise truth-recovery bench -- pinned core environment.
# These are the versions the committed results_merged.json and REPORT.md were
# produced with (Python 3.13). The simulation/scoring core and all ten R-validated
# method ports need only numpy + scipy; matplotlib/reportlab build the figures and
# the manuscript PDF; pytest runs the correctness gate. scikit-learn is needed at
# RUNTIME only to load the pre-trained NPE/Unified model (sbi_model.pkl); it is a
# transitive dependency of the committed artifact, not of the ten core methods.
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
reportlab==4.5.1
pytest==9.0.3
scikit-learn==1.8.0
