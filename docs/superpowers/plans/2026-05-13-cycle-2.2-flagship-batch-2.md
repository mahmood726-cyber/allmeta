# Cycle 2.2 — Flagship batch 2 Implementation Plan (reference plan)

> Reference plan. Cycle 2.1 already shipped the 7 shared modules + the
> per-anchor retrofit pattern. This plan is just the per-anchor work
> repeated for 5 more apps, plus a final verify-and-tag task.

**Spec:** `docs/superpowers/specs/2026-05-13-cycle-2.2-flagship-batch-2-design.md`
**Branch:** `feat/cycle-2.2-flagship-batch-2`

## Tasks

Each anchor task FOLDS what was 4 tasks in Cycle 2.1 into one subagent run:
audit → integrate → tests → sanity. This is doable because the pattern is now well-established.

- [ ] **Task 1: heterogeneity** — full retrofit (audit, integrate the wireable modules, write 3 new pytest tests including R-parity vs `rma.uni REML`, add sanity.spec.mjs, run audit re-scan locally).
- [ ] **Task 2: pet-peese** — same, R-parity vs `regtest(model="lm", predictor="sei")` (PET) + `predictor="vi"` (PEESE).
- [ ] **Task 3: effect-size-converter** — same, R-parity via `metafor::escalc` on 2 conversion modes; CSV upload n/a if input is mode-dependent.
- [ ] **Task 4: dta-sroc** — same, R-parity vs `mada::reitsma`; 6-row fixture.
- [ ] **Task 5: nma-pro-v2** — same, **but** the audit is MANDATORY and present-good features must be preserved (user-flagged).
- [ ] **Task 6: final verification + tag** — re-run triage atlas (5 anchors at Tier 1), re-run runtime audit (5 at OK; no regression in Cycle 2.1 anchors), pytest sweep, Sentinel scan, tag `cycle-2.2-v0.1.0`.

Each anchor task references `forest-plot/RETROFIT_AUDIT.md` + Cycle 2.1's spec + plan for the exact pattern.
