# Pairwiseai — _archive

Previous-version HTML bundles, bak files, and scratch JS copies moved here
during the portfolio cleanup on 2026-04-14. Pairwiseai shared several
files with Truthcert1; identical copies are archived in both folders to
preserve history per folder.

## Contents

- `TruthCert-PairwisePro-v1.0-bundle.backup-*.html` — 2026-01-18 bundle
  hardening snapshots.
- `TruthCert-PairwisePro-v1.0-fast.html.backup-*` — fast-variant
  snapshots.
- `app.js.backup-*`, `app.js.bak` — pre-release JS snapshots.
- `app_beautified.js`, `app_fixed.js`, `app_test.js` — scratch variants.

## Active entry

`Main screen.html` remains the current entry file for this folder. The
hub points there.

## Note for repo pushes

If this folder is pushed to GitHub, add `_archive/` to `.gitignore` first.
